/**
 * Pipeline runner page.
 * Configures and launches emClarity processing workflow steps.
 */
export function WorkflowPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Workflow</h2>
      <p className="mt-2 text-gray-500 dark:text-gray-400">
        Configure and run processing pipeline steps.
      </p>
    </div>
  )
}
