/**
 * Tilt-series table page.
 * Displays and manages the tilt-series data for the current project.
 */
export function TiltSeriesPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Tilt Series</h2>
      <p className="mt-2 text-gray-500 dark:text-gray-400">
        View and manage tilt-series data sets.
      </p>
    </div>
  )
}
