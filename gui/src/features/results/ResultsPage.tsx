/**
 * Results viewer page.
 * Displays FSC curves, reconstruction quality metrics, and outputs.
 */
export function ResultsPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Results</h2>
      <p className="mt-2 text-gray-500 dark:text-gray-400">
        View FSC curves, resolution metrics, and reconstruction results.
      </p>
    </div>
  )
}
